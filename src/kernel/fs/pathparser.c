#include "pathparser.h"
#include "string.h"

#define MAX_PATH_SIZE 128        //TODO: move definition

static int path_validate(char *path){
    int rv = 0;
    //int len = n_strnlen(path, MAX_PATH_SIZE);


    return rv;
}
